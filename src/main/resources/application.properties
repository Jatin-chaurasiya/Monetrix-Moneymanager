# ============================================
# DATABASE CONFIGURATION (MySQL - Local)
# ============================================
spring.datasource.url=jdbc:mysql://localhost:3306/moneymanager?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=8953
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# ============================================
# JPA/HIBERNATE CONFIGURATION (COMMON)
# ============================================
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# ============================================
# SERVER CONFIGURATION (COMMON)
# ============================================
server.servlet.context-path=/api/v1.0
server.port=${PORT:8080}

# ============================================
# EMAIL CONFIGURATION (COMMON)
# ============================================
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${MAIL_USERNAME:jatinkumar638881@gmail.com}
spring.mail.password=${MAIL_PASSWORD:mbuo mwwr ziev edng}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.protocol=smtp

# ============================================
# JWT CONFIGURATION (COMMON)
# ============================================
jwt.secret=${JWT_SECRET:J8f92kLmN55zPfZtQwR8Yn329PsL9vW7}
jwt.expiration=36000000

# ============================================
# URLS (COMMON)
# ============================================
money.manager.frontend.url=${MONEY_MANAGER_FRONTEND_URL:http://localhost:5173}
app.activation.url=${MONEY_MANAGER_BACKEND_URL:http://localhost:8080/api/v1.0}

# ============================================
# GOOGLE OAUTH2 (COMMON)
# ============================================
spring.security.oauth2.client.registration.google.client-name=google
spring.security.oauth2.client.registration.google.client-id=${GOOGLE_CLIENT_ID:14080772032-8ge1i5l8kmr3bujf3faabf7df00scudf.apps.googleusercontent.com}
spring.security.oauth2.client.registration.google.client-secret=${GOOGLE_CLIENT_SECRET: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}
spring.security.oauth2.client.registration.google.redirect-uri=${MONEY_MANAGER_FRONTEND_URL:http://localhost:5173}/auth/google/callback